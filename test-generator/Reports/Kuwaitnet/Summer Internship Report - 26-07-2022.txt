Summer Internship Report (26/07/2022)
Hisham Iqbal Khokhar

Objective - To build a web application that will allow admins to create a storage for test tasks and generate a test suite from them.

- Switched for to a model form which made it easier to manipulate data.
- Made the app accessible to admins only by adding LOGIN_URL
- Modified the Suite model so that a unique code could be generated using random module.
- This code is then appended at the end of a url so that a unique link is made.
- Studied concepts of *args and **kwargs
- Information entered by admin via the form is used and relevent data is rendered frokm MySQL to the django template, which displays the required tasks.
- Modified models so that f-strings are used.